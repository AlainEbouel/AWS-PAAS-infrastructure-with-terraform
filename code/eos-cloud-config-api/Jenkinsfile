pipeline {
    agent {label 'agent'}
    // environment {
    // }
    stages {
        stage('build'){
            steps{
              sh 'cd code/eos-cloud-config-api && ls -l'

              withMaven (
                // mavenLocalRepo: 'code/eos-cloud-config-api'
              ){
                sh "mvn clean verify"
              }
            }
            
        }
    }
}