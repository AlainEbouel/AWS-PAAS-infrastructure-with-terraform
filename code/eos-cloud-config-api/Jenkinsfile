pipeline {
    agent {label 'agent'}
    // environment {
    // }
    stages {
        stage('build'){
            steps{

              sh 'cat ~/.profile'
              sh 'source ~/.profile'
              sh 'env'
              sh 'cd code/eos-cloud-config-api'
              // sh 'mvn clean verify'
              withMaven (
                // mavenLocalRepo: 'code/eos-cloud-config-api'
              ){
                sh "mvn clean verify"
              }
            }
            
        }
    }
}