pipeline {
    agent {label 'agent'}

    stages {
        stage('build'){
            steps{
              sh 'env'
              sh 'cd code/eos-cloud-config-api'
              sh 'mvn clean verify' //-f code/eos-cloud-config-api/pom.xml'             
            }
            
        }
    }
}